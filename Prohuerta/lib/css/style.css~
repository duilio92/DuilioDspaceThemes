/**
 * The contents of this file are subject to the license and copyright
 * detailed in the LICENSE and NOTICE files at the root of the source
 * tree and available online at
 *
 * http://www.dspace.org/license/
 */

/* Hide a tag from visibility. Used in exception2dri.xslt to hide error stack by default. */


/* Preserve whitespace. Used in exception2dri.xslt to display error messages in themes. */
* {
margin: 0;
padding: 0;
}
.clear { clear: both; }
.clearfix {
    overflow: auto;
}

div#ds-main {
				max-width: 1000px;
        margin-left: auto;
        margin-right: auto;
        text-align: left;
        padding: 0;
        height: 100%;
				background-color: white;
				overflow:auto
}

div#ds-header {
				background-color: inherit;
			  width:1000px;
        float: left;
				margin-bottom: 1em;
}

div#banner_cointainer{
       float: left;
    
       padding: 0px 10px 0px 10px;
}
div#ds-options{
				
    		float: left;
				position: relative;
        width: 26,5%;
        color: inherit;
				background-color: inherit;
}
div#ds-body {
		background-color: inherit;
    float: left;
    margin-right: 1em;
    padding-bottom: 5em;
    position: relative;
    width: 72.5%;
}
/*
	FOOTER
*/
	div#ds-footer {
        float: left;
        margin-top: 25px;
    		clear: both;
    		font-size: 60%;
    		margin: auto;
    		text-align: center;
    		width: 100%;
	}
	div.dircn {
    background-image: url("../images/bottom.jpg");
	}
	div.piecat {
    background-color: #eeeeee;
    border-top: 1px solid #cccccc;
    font-size: 13px;
    padding: 10px 10px 0;
    text-align: left;
	}
	div.piecat ul {
    display: inline-table;
    vertical-align: top;
	}
	ul, ol {
    margin: 0 0 10px 25px;
    padding: 0;
	}
	div.piecat ul label {
    font-weight: bold;
    margin: auto;
    list-style: outside none none;
    margin-left: 6px;
	}
	div.creditos {
    background: #f9f9f9 none repeat scroll 0 0;
    border-top: 1px solid #ddd;
    color: #666;
    font-size: 12px;
    line-height: 2.2em;
    padding: 5px;
    text-align: center;
	}
	li {
    line-height: 20px;
	}
	label {
    display: block;
    margin-bottom: 5px;
	}
/*
FIN FOOTER NOTA: REVISAR SI LOS ESTILOS DEL FOOTER QUEDAN BIEN AFECTANDO AL RESTO
*/
/*HEADER*/
nav#access {
    background: black;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
    clear: both;
    display: block;
    float: left;
    margin: 0 auto 6px;
    width: 100%;
}
nav#access ul {
    font-size: 13px;
    list-style: outside none none;
    /*margin: 0 0 0 -0.8125em;*/
    padding-left: 0;
}
#access div{
	margin: 0 6%;
}
.menu li {
    padding-bottom: 1em;
    padding-right: 2.5em;
}
nav#access li {
    float: left;
    position: relative;
}
.menu a {
    color: white;}
body {
    background-color: #0b7baf;
}

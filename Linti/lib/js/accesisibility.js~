/*Script to hide header link except when it is focused*/
window.addEventListener("load", setUp);
function setUp(){
		var hiddenLink = document.getElementById("linkToBody");
		hiddenLink.onFocus(alert("hola"));
}
